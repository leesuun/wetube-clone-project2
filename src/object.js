const object = {
    regExp: {
        passRegex: /((?=.*[0-9])(?=.*[a-zA-Z])|(?=.*[a-zA-Z])(?=.*[0-9]))/g,
        emailRegex: /(^[0-9a-zA-Z]{0,15}@[0-9a-zA-Z]{0,10}.*)/g,
    },
};

export default object;
